# Copy to .env and fill in. Never commit the real .env (it is gitignored).

# Provider API keys — get your own
OPENROUTER_API_KEY=
FAL_KEY=
ELEVENLABS_API_KEY=
PIXABAY_API_KEY=

# Defaults
SCRIPT_DEFAULT_MODEL=openai/gpt-4o-mini
IMAGE_DEFAULT_MODEL=fal-ai/flux/schnell
IMAGE_PRICE_USD=0.003
